{
  "name": "dsh-bridge",
  "displayName": "DSH Bridge",
  "description": "Bridges a VS Code window (embedded code-server or local desktop) to a DeepSeek Harness host: receives ACP-style session updates (file edits with before/after diff), enforces follow mode and file locks.",
  "version": "0.4.0",
  "publisher": "dsh",
  "engines": {
    "vscode": "^1.85.0"
  },
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "受限模式下仅保持连接并提示信任；信任工作区后才开始接收 DSH 编辑同步"
    }
  },
  "main": "./extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "dsh-bridge.reconnect",
        "title": "DSH Bridge: Reconnect"
      },
      {
        "command": "dsh-bridge.toggleFollow",
        "title": "DSH Bridge: Toggle Follow Mode"
      }
    ]
  }
}
